// IP ADDRESS for all hosts
export const HOST_IP = '0.0.0.0';

// Cookie configurations
export const COOKIE_EXPIRATION_TIME = 1000 * 60 * 60; // 1 hour

// Cors Origin options.
export const CORS_ORIGIN = [/localhost/, /\.vercel\.app$/];

// JWT Cookie name.
export const JWT_COOKIE_NAME = 'access_token';

// JWT expiration time.
export const JWT_EXPIRATION_TIME = '1h';
